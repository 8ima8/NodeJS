// .js -> por defecto utiliza common JS
// .mjs -> por defecto utiliza ES6 ES MODULES
// .cjs -> por defecto utiliza common JS

import {sum,sub} from './sum.mjs';

console.log(sum(1,2))

console.log(sub(5,2))